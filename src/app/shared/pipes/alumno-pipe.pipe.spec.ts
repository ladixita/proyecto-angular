import { AlumnoPipePipe } from './alumno-pipe.pipe';

describe('AlumnoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlumnoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
